// src/pages/Nutrition.jsx — meal logging (photo + barcode) on its own screen.
import MealLogger from '../components/MealLogger';

export default function Nutrition() {
  return (
    <div className="page">
      <MealLogger />
    </div>
  );
}
